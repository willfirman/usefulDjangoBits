from .base import *

# Set the Django settings that differ across your environments here.
# You can hardcode them into your settings file, or read them from a file on disk. Or take the 12-factor
# approach and read them in from environment variables.
SECRET_KEY = 'some method to get your production secret key here!'

DEBUG = False
